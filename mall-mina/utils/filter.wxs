var filters = {
  toFix: function(value) {
    console.log(value)
    if (value){
      return Number(value).toFixed(2);
    }else{
      return ''
    }
   
  },
}
module.exports = {
  toFix: filters.toFix
}